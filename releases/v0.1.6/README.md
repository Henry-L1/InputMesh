# InputMesh v0.1.6 verified release record

This release record covers the keyboard-source independence fix. The source
version, tag, platform asset names, platform separation and SHA-256 values have
been verified against the local macOS arm64 build and the Windows x64 build on
the host at `192.168.101.19`.

The record is not a GitHub release or tag publication. Publish only after the
usual release approval.
